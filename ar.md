# دليل إعداد ودجت ملف ديسكورد التعريفي — Discord Profile Widget
تاريخ الكتابة: يونيو 2026
يشرح هذا الدليل كيفية إنشاء ودجت (Widget) مخصص على ملفك التعريفي في ديسكورد باستخدام الإصدار الثاني من الودجت (Widgets v2).
**المتطلبات:** حساب ديسكورد، متصفح كروم (Chrome) أو فايرفوكس (Firefox)، تطبيق Termux (لهواتف أندرويد) أو أداة PowerShell (لويندوز).
> [!WARNING]
> اعتبارًا من **4 يونيو 2026**، فرضت ديسكورد قيودًا على الودجت بحيث **يُسمح لمالك التطبيق (Application Owner) فقط** بإضافة الودجت إلى ملفه التعريفي الخاص. هذا يعني أنه إذا كنت تقوم ببناء هذا الودجت كخدمة لعامة الناس أو لأشخاص آخرين (وليس لنفسك فقط)، فلن يظهر الودجت على الأرجح في ملفاتهم التعريفية بسبب هذا القيد الجديد. يظل هذا الدليل مفيدًا إذا كنت تصنع الودجت لنفسك فقط، بصفتك مالك التطبيق.
> 
## الخطوة 1 — إنشاء التطبيق
 1. انتقل إلى الرابط التالي: discord.com/developers/applications
 2. انقر على زر **New Application** (تطبيق جديد)، ثم أدخل أي اسم تريده، وانقر على **Create** (إنشاء).
 3. احفظ معرف التطبيق (**Application ID**) الموجود في صفحة المعلومات العامة (General Information).
## الخطوة 2 — تمكين حزمة التطوير الاجتماعي (Social SDK)
 1. من الشريط الجانبي، انقر على **Games** (الألعاب)، ثم اختر **Social SDK**.
 2. املأ النموذج الظاهر أمامك وانقر على **Submit** (إرسال).
 3. ستحصل على إمكانية الوصول الفوري والمباشر.
## الخطوة 3 — فتح محرر الودجت
افتح بوابة المطورين (Developer Portal) في متصفحك، واضغط على زر **F12** لفتح أدوات المطور، ثم انتقل إلى علامة التبويب **Console** (وحدة التحكم)، والصق الكود التالي:
```js
let _mods = webpackChunkdiscord_developers.push([[Symbol()],{},r=>r.c]);
webpackChunkdiscord_developers.pop();
let findByProps = (...props) => {
    for (let m of Object.values(_mods)) {
        try {
            if (!m.exports || m.exports === window) continue;
            if (props.every((x) => m.exports?.[x])) return m.exports;
            for (let ex in m.exports) {
                if (props.every((x) => m.exports?.[ex]?.[x]) && m.exports[ex][Symbol.toStringTag] !== 'IntlMessagesProxy') return m.exports[ex];
            }
        } catch {}
    }
}
findByProps("getAll").getAll().find(e=>e.getName() === "ApexExperimentStore").createOverride("2026-03-widget-config-editor", 1)
```
بعد تشغيل الكود، **لا تقم** بإعادة تحميل الصفحة (لا تضغط على F5). فقط انقر على سهم الرجوع للخلف في المتصفح، ثم افتح تطبيقك مجددًا. ستلاحظ ظهور خيار **Widget** أسفل قسم Games في الشريط الجانبي.
## الخطوة 4 — تصميم الودجت
 1. انقر على **Create Widget** (إنشاء ودجت).
 2. صمّم الودجت الخاص بك وأضف الحقول (Fields) التي ترغب بظهورها.
 3. انقر على **Sample Data** في الأسفل وأضف بيانات تجريبية (Demo Data) لكل حقل قمت بإنشائه.
 4. انقر على **Generate JSON** ثم قم بحفظ وتخزين النص الناتج.
 5. انقر على **Save Changes** (حفظ التغييرات)، ثم اضغط على **Publish** (نشر).
## الخطوة 5 — إعداد بروتوكول OAuth2
 1. من الشريط الجانبي، انقر على **OAuth2**.
 2. في قسم روابط إعادة التوجيه (Redirects)، انقر على **Add Redirect** وأدخل الرابط التالي: https://discord.com
 3. انقر على **Save** لحفظ التعديلات.
 4. مرر لأسفل الصفحة للوصول إلى منشئ الروابط (**URL Generator**).
 5. حدد النطاقات التالية فقط (**Scopes**): openid و sdk.social_layer.
 6. اختر رابط إعادة التوجيه (Redirect): https://discord.com.
 7. انسخ الرابط الذي تم إنشاؤه في الأسفل.
 8. في الرابط الذي نسخته، قم بتغيير قيمة المعلمة response_type=code لتصبح response_type=token.
 9. افتح الرابط المعدل في متصفحك، وقم بالموافقة على منح الصلاحيات المطلوبة.
## الخطوة 6 — إصدار هوية تطبيقك (Application Identity)
> [!NOTE]
> هذه الخطوة مطلوبة فقط إذا كان الودجت مخصصًا لملفك الشخصي أنت.
> 
إذا كنت تقوم ببناء هذا الودجت لنفسك فقط (وليس كخدمة لتقديمها للآخرين)، فإن هذه الخطوة **إلزامية** — وبدونها سيعلق الودجت في وضع التحميل مستمرًا في إظهار رسالة: "Your game stats are still syncing. Keep playing!" ولن يظهر لأي مستخدم آخر.
 1. تأكد من قيامك بتفويض وتخويل تطبيقك بنجاح من الخطوة 5، وتحقق من ظهوره في صفحة إعدادات التطبيقات المعتمدة (Authorized Apps) في حسابك على ديسكورد مع كافة الصلاحيات المطلوبة (الرسائل، الأصدقاء، حالة النشاط، إلخ).
 2. من صفحة البوت (**Bot**) في بوابة المطورين، انقر على **Reset Token** (إعادة تعيين الرمز التعريفي) واحفظ التوكن الجديد في مكان آمن.
 3. جهّز بيانات الـ JSON الخاصة بك مع إضافة حقل "username" في الجذر الرئيسي (Root)، على سبيل المثال:
```json
{
    "username": "any",
    "data": {
        "dynamic": [
            {"type": 1, "name": "FIELD_NAME", "value": "FIELD_VALUE"}
        ]
    }
}
```
 4. قم بتشغيل الأمر التالي بناءً على نظام التشغيل الخاص بك (تأكد من استبدال القيم المؤقتة بالقيم الفعلية الخاصة بك):
**أداة PowerShell (على نظام ويندوز):**
```powershell
Invoke-RestMethod -Uri "https://discord.com/api/v9/applications/APPLICATION_ID/users/USER_ID/identities/0/profile" -Method PATCH -Headers @{"Content-Type"="application/json"; "Authorization"="Bot BOT_TOKEN";"User-Agent"="DiscordBot (https://github.com/discord/discord-api-docs, 1.0.0)"} -Body '{"username":"any","data":{"dynamic":[{"type":1,"name":"FIELD_NAME","value":"FIELD_VALUE"}]}}'
```
**تطبيق Termux / أنظمة Linux / macOS:**
```bash
curl -X PATCH "[https://discord.com/api/v9/applications/APPLICATION_ID/users/USER_ID/identities/0/profile](https://discord.com/api/v9/applications/APPLICATION_ID/users/USER_ID/identities/0/profile)" \
-H "Content-Type: application/json" \
-H "Authorization: Bot BOT_TOKEN" \
-H "User-Agent: DiscordBot ([https://github.com/discord/discord-api-docs](https://github.com/discord/discord-api-docs), 1.0.0)" \
-d '{"username":"any","data":{"dynamic":[{"type":1,"name":"FIELD_NAME","value":"FIELD_VALUE"}]}}'
```
> [!IMPORTANT]
>  * أمر curl الذي يستخدم علامة المائلة العكسية \ لتقسيم السطور يعمل فقط على Termux و Bash و Linux و macOS. **ولا يعمل إطلاقًا في PowerShell**. إذا كنت تستخدم PowerShell، فاستخدم أمر Invoke-RestMethod الموضح أعلاه بدلاً منه.
>  * الرموز والأقواس التوضيحية مثل {discordApplicationId} الواردة في المراجع الأخرى هي مجرد نصوص بديلة — يجب عليك إزالة الأقواس المعقوفة { } بالكامل ووضع قيمتك الفعلية الحقيقية مكانها (مثال: استخدام users/1234567890 بدلاً من users/{1234567890}).
>  * تأكد دائمًا من إحاطة الرابط بالكامل بعلامات اقتباس مزدوجة " " في PowerShell لتفادي أي مشاكل ناجمة عن المسافات الفارغة.
> 
إذا تم تنفيذ الأمر بنجاح ودون أخطاء، فتخطّ الخطوة التالية وانتقل مباشرة إلى الخطوة 8. أما إذا كنت تبني خدمة لتقديمها لآخرين (وليس لنفسك فقط)، فتخطّ هذه الخطوة بالكامل.
## الخطوة 7 — إرسال بيانات الودجت (للتحديثات المستقبلية)
هذا الأمر مطابق للأمر الموجود في الخطوة 6 (ولكن بدون إضافة حقل اسم المستخدم "username" في الجذر الرئيسي إذا كنت قد أكملت الخطوة 6 مسبقًا)، وستحتاج لإعادة تشغيله في كل مرة تريد فيها تحديث حقول الودجت وعرض القيم الجديدة.
القيم المطلوبة للاستبدال:
 * APPLICATION_ID: معرف تطبيقك الذي حصلت عليه من بوابة المطورين.
 * USER_ID: معرف حساب ديسكورد الخاص بك (يمكنك الحصول عليه بالانتقال إلى الإعدادات > متقدم > تمكين وضع المطور "Developer Mode"، ثم النقر بزر الماوس الأيمن على اسمك واختيار نسخ المعرف).
 * BOT_TOKEN: التوكن الخاص بالبوت من صفحة Bot في بوابة المطورين بعد النقر على Reset Token.
 * FIELD_NAME و FIELD_VALUE: اسم وقيمة كل حقل مخصص قمت بتصميمه في الودجت.
## الخطوة 8 — إضافة الودجت إلى ملفك التعريفي
> [!WARNING]
> قامت ديسكورد مؤخرًا بتعديل وتحديث طريقة إضافة الودجت إلى الملف الشخصي لتصبح أكثر تعقيدًا، كما أن السكربتات البرمجية المسؤولة عن ذلك يتم تحديثها باستمرار. يُنصح بشدة بالانضمام إلى خادم "Discord Previews" ومتابعة القناة المخصصة للحصول على أحدث سكربت برمجي فعال، بدلاً من الاعتماد على سكربت برمجى ثابت قد يصبح غير صالح للعمل في أي وقت.
> 
الطريقة العامة المتبعة حاليًا (وقد تتطلب التحديث لاحقًا):
افتح ديسكورد عبر المتصفح، واضغط على الأزرار **Ctrl+Shift+I** معًا، وتوجه لعلامة تبويب وحدة التحكم (**Console**)، ثم الصق كودًا مشابهًا للآتي (مع الحرص على جلب النسخة الأحدث من مصدر موثوق):
```js
let _mods=webpackChunkdiscord_app.push([[Symbol()],{},e=>e.c]);webpackChunkdiscord_app.pop();
let findByProps=(...e)=>{for(let t of Object.values(_mods))try{if(!t.exports||t.exports===window)continue;if(e.every(e=>t.exports?.[e]))return t.exports;for(let r in t.exports)if(e.every(e=>t.exports?.[r]?.[e])&&"IntlMessagesProxy"!==t.exports[r][Symbol.toStringTag])return t.exports[r]}catch{}};

api = findByProps("Bo", "Cu").Bo
async function addWidget(appId) {
    id = findByProps("getCurrentUser").getCurrentUser().id;
    current_widgets = (await api.get("/users/" + id + "/profile")).body.widgets
    if (current_widgets.map(x=>x.data?.application_id).includes(appId)) {return console.log("Already in your widgets — remove it via Discord client to re-add")}
    current_widgets.unshift({"data": {"type": "application","application_id": appId}})
    await api.put({url: "/users/@me/widgets",body:{widgets: current_widgets}})
}
// Usage
addWidget("APPLICATION_ID")

```
استبدل النص APPLICATION_ID بمعرف تطبيقك الفعلي، ثم اضغط على زر Enter لتشغيل الكود.
تأكد أيضًا من تفعيل التجربة (Experiment) المسماة 2026-03-application-widget-v2-renderer وضبطها على الخيار **Variant 1**، وإلا فقد لا يظهر الودجت نهائيًا حتى وإن تمت إضافته بنجاح.
بعد ذلك، افتح ملفك الشخصي في ديسكورد، وانقر على خيار **Add Widget** (إضافة ودجت)، واختر تطبيقك المخصص، ثم اضغط على **Save** (حفظ).
> [!NOTE]
> حتى لو نجحت كافة الخطوات السابقة، ونظرًا للقيود الجديدة المفروضة من ديسكورد (المشار إليها في التحذير الرئيسي أعلاه)، فإن الودجت سيظهر **لك أنت فقط بصفتك مالك التطبيق**، ولن يتمكن الزوار الآخرون لملفك التعريفي من رؤيته بالضرورة.
> 
## ملاحظات هامة
 * الخطوات 3 و 5 و 8 هي خطوات تُنفذ لمرة واحدة فقط ولن تحتاج إلى إعادتها مجددًا (باستثناء تحديثات ديسكورد المستمرة للسكربت في الخطوة 8).
 * لتحديث بيانات وقيم الودجت مستقبلاً، ما عليك سوى إعادة تشغيل الأمر الموضح في الخطوة 7 بالبيانات الجديدة.
 * لا تشارك توكن البوت (Bot Token) الخاص بك مع أي شخص على الإطلاق. وفي حال تم تسريبه أو كشفه للعلن، سارع فورًا بالنقر على **Reset Token** لإصدار توكن جديد وإلغاء القديم.
 * لا تشارك رمز الوصول (Access Token) الذي يظهر في رابط الـ OAuth2 مع أي شخص.
 * حاليًا (بعد 4 يونيو 2026)، يعرض الودجت لمالك التطبيق فقط ولا يظهر للزوار الآخرين.
## حل المشكلات الشائعة (PowerShell)
### خطأ: The term '-H' is not recognized... أو أخطاء مشابهة تتعلق بالرموز -d و -X
**السبب:** لقد قمت بنسخ أمر curl بصيغة ومصطلحات أنظمة Linux/Bash (والتي تستخدم علامة \ في نهاية السطور) وقمت بلصقها مباشرة في PowerShell. كلمة curl في PowerShell تُشير تلقائيًا للأمر Invoke-WebRequest وهو لا يدعم وسائط -H أو -X أو -d.
**الحل:** استخدم أمر Invoke-RestMethod المخصص لـ PowerShell (الموجود في الخطوة 6) بدلاً من استخدام أمر curl.
### خطأ: A positional parameter cannot be found that accepts argument '...'
**السبب الأول:** الرابط الموجود بعد المعلمة -Uri غير محاط بعلامات اقتباس مزدوجة " ". أي مسافة فارغة زائدة في الرابط ستؤدي إلى تقسيمه لعدة معاملات منفصلة يحاول نظام PowerShell معالجتها بشكل فردي مما يتسبب في الخطأ.
**السبب الثاني (الأكثر شيوعًا):** نسيان إزالة الأقواس المعقوفة { } المحيطة بالقيم التوضيحية. الأقواس المعقوفة مثل {discordApplicationId} هي مجرد قيم نائبة توضيحية — يجب إزالة القوسين { و } تمامًا وكتابة القيمة الحقيقية مكانهما.

| الحالة | خاطئ | صحيح |
| :--- | :--- | :--- |
| مثال توضيحي | users/{1234567890} | users/1234567890 |

**الحل:** أحِط الرابط بالكامل بعلامات اقتباس مزدوجة " "، وتأكد تمامًا من عدم بقاء أي أقواس معقوفة { أو } في الرابط أو في التوكن الخاص بك.
### خطأ: يظهر التوكن فارغًا بالشكل التالي: "Bot {}" أو "Bot "
**السبب:** وقوع نفس مشكلة الأقواس المعقوفة المذكورة أعلاه، أو أنك نسيت وضع التوكن الحقيقي الخاص بالبوت بدلاً من الكلمة التوضيحية BOT_TOKEN.
**الحل:** تأكد من وضع التوكن الفعلي مباشرة بعد كلمة Bot  (مع ترك مسافة واحدة بينهما فقط) دون أي أقواس معقوفة.
### قائمة التحقق السريعة قبل تشغيل أي أمر
 * [ ] التأكد من إزالة كافة الأقواس المعقوفة { و } من القالب بالكامل.
 * [ ] التأكد من إحاطة الرابط بالكامل بعلامات اقتباس مزدوجة " ".
 * [ ] كتابة الأمر في سطر واحد مستمر (أو تعريف متغيرات PowerShell للأسطر بشكل صحيح، وعدم استخدام رمز المائلة العكسية \).
 * [ ] استخدام أمر Invoke-RestMethod إذا كنت تعمل على PowerShell، أو أمر curl إذا كنت تعمل على Termux/Linux/macOS — وتجنب الخلط بينهما.
